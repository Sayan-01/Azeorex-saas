import React from "react";

const Toolbar = () => {
  return (
    <div className="flex flex-col gap-4 items-center">
      <div onClick={() => {}}>select</div>
      <div onClick={() => {}}>text</div>
      <div onClick={() => {}}>rect</div>
      <div onClick={() => {}}>ellipse</div>
      <div onClick={() => {}}>pec</div>
    </div>
  );
};

export default Toolbar;
